#!groovy
pipeline { 
    agent any 
    options {
        skipStagesAfterUnstable()
    }
    stages {
        stage('Build') { 
            steps { 
                gradle build 
                junit 'reports/**/*.xml' 
            }
        }
        stage('Test'){
            steps {
                echo integration tests
            }
        }
        stage('Deploy') {
            steps {
                gradle run
            }
        }
    }
}